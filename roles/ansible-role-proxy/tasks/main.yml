---

- name: Create and run gm-control binary as a service
  include_role:
    name: ansible-role-systemd
    public: yes
    apply:
      tags:
        - proxy
  when: ( ansible_distribution == 'Ubuntu' )
  tags: proxy